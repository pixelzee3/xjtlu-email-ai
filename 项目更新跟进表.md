# 项目更新跟进表

用于对照 **Git 仓库当前提交（HEAD）** 记录工作区改动规模，便于和组员同步或写周报时快速引用。

## 使用说明

- **基准**：下列统计以本地 `HEAD` 为准（当前分支最后一次提交）。刷新方式：

```bash
git status --short
git diff --stat HEAD
git ls-files --others --exclude-standard
```

- 将本表中的「快照日期」在每次大改后手动更新，并粘贴最新 `git diff --stat` 输出到「附录」。

---

## 快照（请随提交更新）

| 项目 | 内容 |
|------|------|
| **快照日期** | 2026-03-25 |
| **当前分支** | `main` |
| **基准提交** | `e30fec6` — feat: LLM batching, cookie fixes, customizable hero slogan (plain/md/html) |

### 仓库清理（相对此前工作区说明）

- 已删除**未跟踪**的临时/调试文件，避免与主线混淆：`src/main_test_50.py`（旧版/试验性 `main` 副本）、`src/run_test.py`（本地 mock 跑 `main`）、`src/verify_deep.py` + `src/verify_result.json`（深度列表验证脚本与输出）、根目录 `test(195).txt`（调试导出）、根目录 Cookie 导出文本。
- [`.gitignore`](.gitignore) 已增加 `cookies_mail*.txt`，降低误提交邮箱 Cookie 导出的风险。

### 已跟踪文件的改动（相对 HEAD）

| 文件 | 变更量（git 显示） | 摘要 |
|------|---------------------|------|
| [`.gitignore`](.gitignore) | 少量行 | 忽略敏感配置、`cookies_mail*.txt` 等 |
| [`README.md`](README.md) | 约 21 行量级 | 登录、`user.db`、启动与开发说明 |
| [`src/app.py`](src/app.py) | 大量 | 本地登录、按用户配置、会话与主流程等 |
| [`src/auth_db.py`](src/auth_db.py) | 新增 | SQLite 用户库与按用户配置 |
| [`src/main.py`](src/main.py) | 大量 | 邮件抓取与 LLM 流程、与 Web 端协同 |
| [`src/requirements.txt`](src/requirements.txt) | 少量 | 如 `bcrypt` 等 |
| [`src/templates/index.html`](src/templates/index.html) | 大量 | 用户控制台与前端交互 |
| [`src/templates/login.html`](src/templates/login.html) | 新增 | 登录页 |
| [`src/templates/register.html`](src/templates/register.html) | 新增 | 注册页 |
| [`校友产品优先级计划.md`](校友产品优先级计划.md) | 新增/修订 | 产品阶段优先级 |
| [`项目完整介绍与文件说明.txt`](项目完整介绍与文件说明.txt) | 新增/修订 | 项目与文件说明 |
| [`项目更新跟进表.md`](项目更新跟进表.md) | 本文件 | 跟进表自身 |

> **合计（`git diff --stat HEAD`）**：以本地执行结果为准，最近一次统计约为 **`12 files changed, 4065 insertions(+), 605 deletions(-)`**（见附录）。

### 未纳入 Git 的新增文件（相对 HEAD）

以 `git status` 为准；清理后不应再出现上述已删除的临时脚本名。若新增文档或配置，请补登本表并考虑是否应提交。

---

## 附录：原始命令输出（可复制更新）

```
# 2026-03-25 生成（示例）
git status --short
（略，以本机为准）

git diff --stat HEAD
 .gitignore                                         |    3 +
 README.md                                          |   21 +-
 src/app.py                                         | 1358 +++++++++++++----
 src/auth_db.py                                     |  241 +++
 src/main.py                                        |  999 +++++++++---
 src/requirements.txt                               |    1 +
 src/templates/index.html                           | 1590 ++++++++++++++++++--
 src/templates/login.html                           |   99 ++
 src/templates/register.html                        |  102 ++
 ...205\210\347\272\247\350\256\241\345\210\222.md" |   96 ++
 ...26\207\344\273\266\350\257\264\346\230\216.txt" |   67 +
 ...226\260\350\267\237\350\277\233\350\241\250.md" |   93 ++
 12 files changed, 4065 insertions(+), 605 deletions(-)
```

---

## 与「上次提交」相比改了「多少」的一行话

- **总改动文件**：共 **12** 个（相对 `e30fec6` 的未提交改动统计）。
- **改动规模**：约 **+4065 / -605** 行（以附录 `git diff --stat` 为准）。
- **新增文件**：含 `auth_db.py`、登录/注册模板、三份项目文档等；临时测试脚本已移出工作区。
